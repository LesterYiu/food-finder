import Form from "./Form";

const Gallery = () => {

    return(
        <section className="gallerySection">
            <div className="wrapper">
                <Form/>
            </div>
        </section>

    )
}

export default Gallery;